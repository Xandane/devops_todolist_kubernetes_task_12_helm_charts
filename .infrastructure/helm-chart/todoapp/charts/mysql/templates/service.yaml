apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-service
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - port: 3306
      targetPort: 3306
      name: mysql
  selector:
    app: {{ .Chart.Name }}
